language: node_js

node_js:
  - "6"
  - "5"
  - "4"
  - "iojs"
  - "0.12"

os:
  - linux

cache:
  bundler: true
  directories:
  - node_modules

sudo: false

install:
  - npm install

script:
  - npm test

after_success:
  - bash <(curl -s https://codecov.io/bash)
